# config.yaml
vertex_settings:
  project: "test-project-493209"  # Replace with your GCP project ID
  location: "global"   # Replace with your GCP location

server:
  host: "0.0.0.0"
  port: 8080
  debug: false          # 开启后记录详细的 JSON 请求/响应
  log_to_file: true    # 是否输出到 logs/vertex-oai-py.log
  max_log_lines: 1000 # 日志文件最大保留行数
