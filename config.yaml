# config.yaml
vertex_settings:
  project: "test-project-493209"  # Replace with your GCP project ID
  location: "us-central1"   # Replace with your GCP location

server:
  host: "0.0.0.0"
  port: 8080
